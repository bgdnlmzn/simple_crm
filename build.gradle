// Корневой build.gradle для multi-module проекта
plugins {
    id 'java'
}

allprojects {
    group = 'ru.cft.crm'
    version = '0.0.1'
}

subprojects {
    apply plugin: 'java'
    
    repositories {
        mavenCentral()
    }
    
    java {
        sourceCompatibility = '17'
    }
}
