server:
    port: 8080
spring:
    datasource:
        url: jdbc:postgresql://localhost:5430/crm
        username: crm
        password: crm
    flyway:
        enabled: true
        locations:
            - db.migration
